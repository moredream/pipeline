

<div class="row">
  <aside class="col-xs-6 col-md-4">
      <section>
      <h1>
        <%= image_tag @user.image, alt: @user.username, class: "profile" %>

        <%= image_tag @user.image_url(:thumb), alt: @user.username, class: "profile" %>

        <%= @user.username %>

      </h1>
    </section>
  </aside>
  <div class="col-xs-12 col-sm-6 col-md-8">
      <%= link_to @user.username, edit_user_path(@user) %>
  </div>
</div>
