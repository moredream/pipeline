<%= form_for [@commentable, @comment], remote: true  do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_area :content, :class => 'form-control', :placeholder=>"Comments" %>
  <%= f.submit 'Comment', :class=> "btn btn-default" %>

<% end %>
